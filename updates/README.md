# EdgeFit update manifests

Machine-readable release pointers:

- `stable.json`
- `beta.json`
- `dev.json`

They are generated from the private `Rekting/EdgeFit-Source` repository after
build and release validation.
