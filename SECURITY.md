# EdgeFit Security

Only EdgeFit binaries published in this repository's GitHub Releases and
referenced by the official update manifests should be treated as official
builds.

Published packages include SHA-256 checksums.

Future stable releases are planned to use code signing in addition to
checksums.
